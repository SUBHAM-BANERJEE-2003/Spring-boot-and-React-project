server:
  port: 8180

spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/userspring
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true